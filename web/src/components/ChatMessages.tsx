import { Box } from '@chakra-ui/layout'
import React from 'react'

export default function ChatMessages() {
    return (
        <Box display={'flex'} flex={1} flexDirection={'column'} flexBasis={"80%"}>
            Chat
        </Box>
    )
}